# HTTP-MQTT Reverse Proxy Tunnel

HTTP реверс-прокси туннель на базе [comqtt](https://github.com/wind-c/comqtt) - как ngrok, но через MQTT!

## Описание

Этот проект позволяет туннелировать HTTP трафик через MQTT, создавая реверс-прокси для локальных HTTP сервисов. Клиенты подключаются к публичному серверу, который перенаправляет HTTP запросы через MQTT брокер.

### Как это работает

```
┌─────────────┐     HTTP      ┌──────────────┐     MQTT      ┌───────────────┐     HTTP      ┌──────────────┐
│ HTTP Client │──────────────▶│ Tunnel Server│◀─────────────▶│ MQTT Broker   │◀─────────────▶│Local HTTP    │
│   (curl)    │◀──────────────│  (Reverse    │               │  (comqtt)     │               │Server        │
└─────────────┘               │   Proxy)     │               │               │               │(localhost)   │
                              └──────────────┘               └───────────────┘               └──────────────┘
                                                                                    ▲                  ▲
                                                                                    │                  │
                                                                                    └──────────────────┘
                                                                                      Tunnel Client
```

**Процесс:**
1. Клиент туннеля подключается к MQTT брокеру с уникальным ID
2. HTTP запрос приходит на сервер туннеля по пути `/<client-id>/path`
3. Сервер отправляет запрос клиенту через MQTT
4. Клиент форвардит запрос на локальный HTTP сервер
5. Ответ возвращается обратно через MQTT
6. Сервер отдает ответ исходному HTTP клиенту

## Установка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd mqtt-http-tunnel

# Установите зависимости
go mod download

# Соберите все компоненты
make build
```

Будут созданы:
- `bin/server` - туннель сервер (MQTT брокер + HTTP реверс-прокси)
- `bin/client` - туннель клиент (проксирует запросы к локальному серверу)

## Быстрый старт

### 1. Запустите сервер туннеля

```bash
./bin/server
```

Вывод:
```
2025/11/20 19:30:00 MQTT broker started on port 1883
2025/11/20 19:30:01 HTTP reverse proxy connected to MQTT broker
2025/11/20 19:30:01 HTTP reverse proxy started on port 8080
2025/11/20 19:30:01 Ready to forward requests to clients via /<client-id>/*
```

### 2. Запустите тестовый HTTP сервер (в другом терминале)

```bash
go run examples/test_server.go
```

Это запустит простой HTTP API сервер на `http://localhost:3000`

### 3. Запустите клиент туннеля (в третьем терминале)

```bash
./bin/client -client-id=my-app -target=http://localhost:3000
```

Вывод:
```
2025/11/20 19:30:05 Starting HTTP tunnel client...
2025/11/20 19:30:05   Client ID: my-app
2025/11/20 19:30:05   MQTT Broker: tcp://localhost:1883
2025/11/20 19:30:05   Target: http://localhost:3000
2025/11/20 19:30:05
2025/11/20 19:30:05 Your local server is now accessible via:
2025/11/20 19:30:05   http://tunnel-server:8080/my-app/
2025/11/20 19:30:05 Tunnel established! Waiting for requests...
```

### 4. Тестируйте туннель (в четвертом терминале)

```bash
# Простой GET запрос
curl http://localhost:8080/my-app/api/hello

# Ответ:
# {"message":"Hello from test server!","status":"ok"}

# POST запрос
curl -X POST http://localhost:8080/my-app/api/echo \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello through MQTT!"}'

# Или используйте demo скрипт
./examples/demo.sh my-app
```

## Использование

### Сервер туннеля

```bash
./bin/server [options]
```

**Параметры:**
- `-mqtt-port` - порт MQTT брокера (по умолчанию: 1883)
- `-http-port` - порт HTTP прокси (по умолчанию: 8080)

**Endpoints:**
- `GET /health` - health check
- `ALL /<client-id>/*` - проксирует запросы клиенту с указанным ID

### Клиент туннеля

```bash
./bin/client -client-id=<ID> -target=<LOCAL_URL> [options]
```

**Обязательные параметры:**
- `-client-id` - уникальный ID клиента (используется в URL для маршрутизации)
- `-target` - URL локального HTTP сервера для проксирования

**Опциональные параметры:**
- `-server` - URL MQTT брокера (по умолчанию: tcp://localhost:1883)

**Примеры:**

```bash
# Туннель для локального веб-приложения на порту 3000
./bin/client -client-id=webapp -target=http://localhost:3000

# Туннель для API сервера на порту 8000
./bin/client -client-id=api -target=http://localhost:8000

# Подключение к удаленному MQTT брокеру
./bin/client -client-id=myapp -target=http://localhost:3000 \
  -server=tcp://mqtt.example.com:1883
```

## Примеры использования

### Пример 1: Экспозиция локального API

У вас есть локальный REST API на порту 5000:

```bash
# Терминал 1: Запустите сервер туннеля
./bin/server

# Терминал 2: Запустите ваш API
python -m flask run --port=5000

# Терминал 3: Создайте туннель
./bin/client -client-id=my-api -target=http://localhost:5000

# Терминал 4: Тестируйте через туннель
curl http://localhost:8080/my-api/users
```

### Пример 2: Несколько туннелей одновременно

Вы можете запустить несколько клиентов с разными ID:

```bash
# Клиент 1: Веб-приложение
./bin/client -client-id=frontend -target=http://localhost:3000

# Клиент 2: API Backend
./bin/client -client-id=backend -target=http://localhost:8000

# Доступ:
curl http://localhost:8080/frontend/
curl http://localhost:8080/backend/api/users
```

### Пример 3: Webhook для разработки

Тестирование webhooks локально:

```bash
# Запустите туннель для вашего сервера
./bin/client -client-id=webhook-test -target=http://localhost:4000

# Настройте webhook в сервисе на:
http://your-tunnel-server:8080/webhook-test/webhook

# Все webhook запросы будут приходить на ваш локальный сервер!
```

### Пример 4: Демонстрация клиенту

```bash
# Запустите туннель для вашего приложения
./bin/client -client-id=demo -target=http://localhost:3000

# Отправьте клиенту ссылку:
http://your-server:8080/demo/

# Клиент видит ваше локальное приложение через туннель!
```

## Архитектура

### Компоненты

1. **MQTT Broker (comqtt)** - легковесный брокер для обмена сообщениями
2. **HTTP Reverse Proxy** - принимает HTTP запросы и маршрутизирует через MQTT
3. **Tunnel Client** - получает запросы через MQTT и проксирует на локальный сервер

### Потоки данных

**HTTP Request Flow:**
```
Client -> Server (/client-id/path)
       -> MQTT (tunnel/client-id/request)
       -> Tunnel Client
       -> Local HTTP Server
```

**HTTP Response Flow:**
```
Local HTTP Server -> Tunnel Client
                  -> MQTT (tunnel/client-id/response)
                  -> Server
                  -> Client
```

### MQTT Topics

- `tunnel/<client-id>/request` - запросы от сервера к клиенту
- `tunnel/<client-id>/response` - ответы от клиента к серверу

## Структура проекта

```
.
├── cmd/
│   ├── server/              # Туннель сервер
│   │   └── main.go
│   └── client/              # Туннель клиент
│       └── main.go
├── pkg/
│   └── tunnel/
│       ├── tunnel.go        # Старый MQTT туннель (deprecated)
│       └── http_tunnel.go   # HTTP реверс-прокси
├── examples/
│   ├── test_server.go       # Тестовый HTTP сервер
│   ├── demo.sh              # Демонстрационный скрипт
│   └── test.sh              # Старые тесты (deprecated)
├── Makefile
├── README.md
└── QUICKSTART.md
```

## Особенности

- ✅ Полное проксирование HTTP (метод, заголовки, тело, статус)
- ✅ Поддержка всех HTTP методов (GET, POST, PUT, DELETE, etc.)
- ✅ Множественные клиенты с разными ID
- ✅ Request/Response correlation через уникальные ID
- ✅ Таймауты (30 секунд по умолчанию)
- ✅ QoS 1 для надежной доставки
- ✅ Автоматическое переподключение к MQTT
- ✅ Легковесный (не требует внешних зависимостей кроме Go)

## Конфигурация

### Production deployment

Для продакшн окружения рекомендуется:

1. **TLS для MQTT:**
```bash
# Настройте comqtt с TLS сертификатами
```

2. **Аутентификация:**
```bash
# Добавьте auth хук в comqtt для проверки клиентов
```

3. **Rate limiting:**
```bash
# Добавьте rate limiting в HTTP прокси
```

4. **Мониторинг:**
```bash
# Логируйте метрики и используйте /health endpoint
```

## Сравнение с аналогами

| Функция | MQTT Tunnel | ngrok | cloudflared |
|---------|-------------|-------|-------------|
| Открытый исходный код | ✅ | ❌ | ✅ |
| Самостоятельный хостинг | ✅ | ❌ | ⚠️ |
| Протокол | MQTT | Proprietary | HTTP/2 |
| Легковесность | ✅ | ❌ | ⚠️ |
| Множественные клиенты | ✅ | ✅ | ✅ |

## Ограничения

- Максимальный размер payload MQTT (по умолчанию ~256MB в comqtt)
- Таймаут запроса: 30 секунд
- WebSocket туннелирование не поддерживается (пока)
- Нет встроенного TLS (нужно настраивать отдельно)

## Roadmap

- [ ] WebSocket проксирование
- [ ] TLS/SSL поддержка
- [ ] Аутентификация клиентов
- [ ] Web UI для управления туннелями
- [ ] Метрики и мониторинг
- [ ] Rate limiting
- [ ] Компрессия трафика

## EventLog - Экспорт и импорт событий

EventLog теперь поддерживает экспорт и импорт событий в формате **JSON Lines (JSONL)** для бэкапов и просмотра.

### Быстрый старт

```bash
# Сборка CLI инструмента
make build-eventlog-tool

# Экспорт всех событий
./bin/eventlog-tool export -file backup.jsonl

# Экспорт за последнюю неделю
./bin/eventlog-tool export \
  -file backup_week.jsonl \
  -start "2025-11-17T00:00:00Z"

# Импорт событий
./bin/eventlog-tool import -file backup.jsonl

# Просмотр через jq
grep -v "^# " backup.jsonl | jq
```

### Возможности

- ✅ Экспорт/импорт в JSONL формате
- ✅ Фильтрация по времени, коллекции, автору
- ✅ Потоковая обработка (низкое потребление памяти)
- ✅ Валидация подписей при импорте
- ✅ Детальные отчеты об ошибках
- ✅ Совместимость с Unix-утилитами (grep, jq, awk)

### Документация

- 📖 [Полная документация по экспорту/импорту](internal/eventlog/EXPORT.md)
- 📖 [EventLog README с примерами](internal/eventlog/README.md)
- 📖 [CLI инструмент - руководство](cmd/eventlog-tool/README.md)

### Примеры использования

```go
// В коде
file, _ := os.Create("backup.jsonl")
err := eventLog.Export(ctx, file, &eventlog.ExportOptions{
    Collection:      "users",
    IncludeMetadata: true,
})

result, _ := eventLog.ImportFromFile(ctx, "backup.jsonl", &eventlog.ImportOptions{
    SkipDuplicates: true,
    UpdateHeads:    true,
})
```

```bash
# Просмотр метаданных
head -n 1 backup.jsonl | sed 's/^# //' | jq

# Статистика по коллекциям
grep -v "^# " backup.jsonl | jq -r '.collection' | sort | uniq -c

# Инкрементальный бэкап
./bin/eventlog-tool export \
  -file "backup_$(date +%Y%m%d).jsonl" \
  -start "$(cat last_backup_time.txt)"
```

## Зависимости

- [comqtt v2](https://github.com/wind-c/comqtt) - MQTT брокер
- [paho.mqtt.golang](https://github.com/eclipse/paho.mqtt.golang) - MQTT клиент
- [xid](https://github.com/rs/xid) - генерация уникальных ID

## FAQ

**Q: Можно ли использовать внешний MQTT брокер?**
A: Да! Просто укажите его URL в параметре `-server` клиента и не запускайте встроенный сервер.

**Q: Какой QoS используется?**
A: QoS 1 (at least once) для гарантии доставки сообщений.

**Q: Можно ли туннелировать HTTPS?**
A: Да, клиент может проксировать на HTTPS endpoint: `-target=https://localhost:443`

**Q: Поддерживается ли HTTP/2?**
A: Нет, только HTTP/1.1

## Лицензия

MIT

## Автор

Создано с использованием [comqtt](https://github.com/wind-c/comqtt)
